let options = {
  strings: [
    "a Software Engineer",
    "a Problem Solver",
    "a Frontend Developer",
    "a Freelancer",
  ],
  shuffle: true,
  showCursor: false,
  loop: true,
  typeSpeed: 30,
  backSpeed: 60,
};

let typed = new Typed("#intro-span", options);
